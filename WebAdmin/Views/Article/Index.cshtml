@model List<Blog>
@{ 
    Layout = "_LeftNav";
}
<table class="table table-striped table-hover table-bordered"> 

    <thead>
        <tr>
            <th>标题</th>
            <th>时间</th>
            <th>操作</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var blog in Model)
        {
            <tr>
                <td>
                    @blog.Title
                </td>
                <td>
                    @blog.UpdateTime
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@blog.Id" class="label label-success">编辑</a>
                    <a asp-action="DeleteArticle" asp-route-id="@blog.Id" class="label label-danger">删除</a>
                </td>
            </tr>
        }
    </tbody>

</table>

<pager pager-option="ViewBag.Pager as MyPagerOption"></pager>
